#include <stdio.h> //não precisa incluir nos outrod doc


/*função que realiza a soma de 2 numeros*/
float soma(float * num1, float * num2);
/*função que realiza a subtração de 2 numeros*/
float subtracao(float * num1, float * num2);
/*funcao que realiza a multiplicação de 2 numeros*/
float multiplicacao(float * num1, float * num2);
/*funcao que realiza a dvisao de 2 numeros*/
float divisao(float * num1, float * num2);